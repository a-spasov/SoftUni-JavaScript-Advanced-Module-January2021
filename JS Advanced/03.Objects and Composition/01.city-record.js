function solve(name, population, treasury) {
    let cityInfo = {};
    cityInfo.name = name,
    cityInfo.population = population,
    cityInfo.treasury = treasury
    return cityInfo;
}
solve("as", 123, 234);